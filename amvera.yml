---
version: null
meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  dockerfile: Dockerfile
run:
  persistenceMount: /app/backend/data
  containerPort: 8080
serviceType: compute
